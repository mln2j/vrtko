////  LoadingScreen.swift//  Vrtko////  Created by Mihael Lendvaj on 03.06.2025..//import SwiftUIstruct LoadingScreen: View {    var body: some View {        ZStack {            // Siva podloga            Color(.systemGray6)                .ignoresSafeArea()            VStack(spacing: 24) {                Spacer()                // Zeleni logo (zamijeni "leaf.fill" svojim SF Symbolom ili custom slikom)                Image(systemName: "leaf.fill")                    .resizable()                    .scaledToFit()                    .frame(width: 80, height: 80)                    .foregroundColor(.primaryGreen) // koristi tvoju zelenu boju                // Ime aplikacije                Text("appName")                    .font(.system(size: 38, weight: .bold))                    .foregroundColor(.primaryGreen)                // Moto aplikacije                Text("motto")                    .font(.headline)                    .foregroundColor(.secondary)                    .padding(.top, 2)                Spacer()                // Na dnu: loading tekst                VStack {                    ProgressView()                        .progressViewStyle(CircularProgressViewStyle(tint: .primaryGreen))                        .scaleEffect(1.2)                    Text("Učitavanje...") // ili "Loading..."                        .font(.subheadline)                        .foregroundColor(.secondary)                        .padding(.top, 4)                }                .padding(.bottom, 32)            }            .padding(.horizontal, 32)        }    }}